export class Location {
  public name: string;
  public lat: string;
  public len: string ;

  constructor(name: string, lat: string, len: string) {
    this.name = name;
    this.lat = lat;
    this.len = len;

  }
}
